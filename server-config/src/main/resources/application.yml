server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vaninagodoydh/ejemplo-docker
          default-label: main
          search-paths: configs
